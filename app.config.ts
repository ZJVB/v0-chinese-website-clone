export default defineAppConfig({
  ui: {
    colors: {
      primary: 'orange',
      neutral: 'slate',
    },
    button: {
      defaultVariants: {
        color: 'primary',
      },
    },
  },
})
