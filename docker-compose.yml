version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: azworld-website
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    # 如果有环境变量，在此添加
    # env_file:
    #   - .env.production
